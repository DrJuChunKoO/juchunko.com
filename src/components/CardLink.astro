---
const { post, lang, category } = Astro.props;
---

<a
	href={`/${lang}/${category}/${post.id.replace(/^.+?\//, "").replace(/\.(md|mdx)$/, "")}`}
	class="group relative flex flex-col items-start overflow-hidden rounded-lg bg-gray-50 p-0.5 transition-colors hover:bg-gray-100 md:p-4 md:pr-24 dark:bg-white/5 dark:hover:bg-white/10"
>
	<div class="bg-primary/5 dark:bg-primary/7.5 flex h-16 w-full items-center justify-center rounded-lg text-3xl md:hidden">
		{post.data.emoji}
	</div>
	<div class="p-2 md:p-0">
		<h2 class="text-sm font-semibold md:text-lg">{post.data.title}</h2>
		<p class="text-xs tracking-tight text-gray-500 md:text-sm">
			{
				post.data.date.toLocaleDateString(lang, {
					year: "numeric",
					month: "long",
					day: "numeric",
				})
			}
		</p>
	</div>
	<div class="absolute right-0 bottom-0 hidden size-24 items-end justify-end pr-4 drop-shadow-lg drop-shadow-black/10 md:flex">
		<div
			class="dark:group-hover:bg-bg-white/10 flex h-32 w-16 translate-y-16 rotate-6 items-start justify-center rounded-lg border border-black/5 bg-gray-50 pt-3 text-4xl transition-all group-hover:translate-y-14 group-hover:rotate-0 group-hover:bg-gray-100 dark:border-white/5 dark:bg-white/5"
		>
			{post.data.emoji}
		</div>
	</div>
</a>
